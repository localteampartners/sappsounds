# Intent — sappsounds

<!-- Captured at scaffold time from the sappcode spec. Read this on first
     Claude run to seed SPEC.md, ARCHITECTURE.md, and TODO.md, then leave
     this file alone as the original brief. -->

Own every generic sampler concern once — parser, mapping, voices, cache, streaming interfaces, diagnostics — so products like SappOrchestra only add product policy on top.
